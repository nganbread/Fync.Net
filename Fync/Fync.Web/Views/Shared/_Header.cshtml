@using Fync.Service
@{
    var currentUser = ViewBag.CurrentUser as ICurrentUser;
}

@if (currentUser.User == null)
{
    Html.Partial("_AuthenticatedHeader");
}
else
{
    Html.Partial("_AnonymousHeader");
}